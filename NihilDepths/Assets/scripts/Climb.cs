using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityStandardAssets.Characters.FirstPerson;

public class Climb : MonoBehaviour 
{
/* 
	public GameObject go;
	public float climbSpeed = 10f;
	public bool climbing;

	void Update()
	{
		if (climbing) 
			{
			// This might work. Maybe. Eh. If you've got time.
			gameObject.GetComponent<Rigidbody>().isKinematic = false;
			if (Input.GetKeyDown(KeyCode.W))
				{
				Vector3 tmpPos = transform.position;
				tmpPos.y = tmpPos.y + climbSpeed;
				transform.position = tmpPos;
				print (tmpPos);
				//gameObject.transform = new Vector3 (0,climbSpeed,0);
				}
			//go.GetComponent < FirstPersonController > ().enabled = false;
			//float v = Input.GetAxis ("Vertical");
			//if (v != 0) 
			//{
			//	go.transform.Translate (Vector3.up * v * climbSpeed * Time.deltaTime);
			//}
			}

		else
		{
			gameObject.GetComponent<Rigidbody>().isKinematic = true;
			//go.GetComponent<FirstPersonController> ().enabled = true;
		}
	}*/
}